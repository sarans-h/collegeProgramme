#include<stdio.h>
#include<math.h>
int main(){
    float a;
    printf("Enter no whose sqrt has to be find:");
    scanf("%f",&a);
    
    printf("SQUARE ROOT OF %.2f IS %.2f",a,sqrt(a));

    return 0;
}